
extends layout

block head-bar
	if user.name
		h3 Welcome #{user.name}
		h3
			a(href="/my-polls").logged-in-link My Polls
			a(href="/new-poll").logged-in-link New Poll
	else
		h3 To create a poll, 
			a(href="/signin") Sign In With Twitter
			|.
		h3 Or Browse through existing polls and vote!

block page
	include all-polls.pug
